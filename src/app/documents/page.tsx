/// A reserved file name page needs to exist for the documents e.g for them to transform documents folder to route. It needs to
//  - a reserved filename
// it should be in the app folder
// It needs to have a default export
//
//

function DocumentsPage() {
  return (
    <div>
      <p>Hello Docs</p>
    </div>
  );
}

export default DocumentsPage;
